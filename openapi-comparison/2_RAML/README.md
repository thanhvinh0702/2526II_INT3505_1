# How to run
## Setup
`pip install -r requirements.txt`
## Start flask server
`python server.py`
## Visualize api docs
`npx raml2html api.raml > index.html -y`  
`npx serve .`
## Access api doc at
`localhost:3000`
